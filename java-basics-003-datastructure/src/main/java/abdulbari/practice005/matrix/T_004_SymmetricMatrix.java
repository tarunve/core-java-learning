package abdulbari.practice005.matrix;

/**
 *	Symmetric Matrix
 * 	----------------
 *	->  Symmetric Matrix is defined when element at [1,j] = element at [j,i]
 *  	    i.e. if M[i,j] = M[j,i]
 *	->	If I know element at [i,j] , it means we know element at [j,i].
 *		So, either we can store elements at lower or upper triangular matrix.
 */
public class T_004_SymmetricMatrix{
	
}